# Copy this to .env locally and fill values (DO NOT commit secrets)
# Backend environment variables
PORT=4000
MONGO_URL=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority
JWT_SECRET=your_jwt_secret_here
STRIPE_SECRET_KEY=sk_test_...
FRONTEND_URL=https://your-frontend-url.onrender.com

# Notes:
# - Keep the real .env out of source control. The repo's root .gitignore already ignores .env files.
# - On Render, set these as Environment -> Environment Variables for your Services.
